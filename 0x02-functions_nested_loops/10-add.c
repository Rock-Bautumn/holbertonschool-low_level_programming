#include "main.h"

/**
 * add - adds two numbers
 * @n: this is the first number we are adding
 * @o: this is the second number we are adding
 * Return: the sum of the numbers
 */

int add(int n, int o)
{
        n= n + o;
	return (n);
}
