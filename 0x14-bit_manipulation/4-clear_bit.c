#include "main.h"

/**
 * clear_bit - it returns 98
 * @n: integer
 * @index: integer
 * Return: always 98
 */
int clear_bit(unsigned long int *n, unsigned int index);
{
	(void) n;
	(void) index;
	return (98);
}
